# CursorColdStart 配置

本目录包含 CursorColdStart 的项目配置。

## 配置文件

- `config/project.json` - 项目基本信息
- `config/technology.json` - 技术栈配置
- `config/packs.json` - 功能包配置

## 使用

```bash
# 更新规则
coldstart update

# 列出可用功能包
coldstart list packs
```
